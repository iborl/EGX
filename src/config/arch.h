#ifndef EGX_ARCH_CONFIG_H
#define EGX_ARCH_CONFIG_H

// Enables AMD64 code paths and typing
#define CONFIG_ARCH_AMD64

// Defining CONFIG_ARCH_IA32 would enable IA32 code paths and typing
// #define CONFIG_ARCH_IA32

// So far, enabling an architecture macro would not disable or enable code paths yet, it only decides what type definitions are used

#endif
